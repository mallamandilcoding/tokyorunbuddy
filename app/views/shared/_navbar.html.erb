<div class="navbar navbar-expand-sm navbar-lewagon bg-<%= @color %>">
  <div class="container-fluid">
  <% if user_signed_in? %>
    <%= cl_image_tag(current_user.photo.key, class: "avatar dropdown-toggle me-3", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false) %>
   <% end %>
  <div class="dropdown-menu dropdown-menu-end new-dropdown" aria-labelledby="navbarDropdown">

    <%= link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>

  </div>
    <%#= link_to suggestions_path, class: "navbar-brand" do %>
      <%#= image_tag "TRB.png" %>
    <%# end %>
    <div class= "message-div">
      <% if user_signed_in?  %>
      <p><%= @message1 %><%= current_user.first_name %></p>
      <p><%= @message2 %></p>
      <% end %>
    </div>
  </div>
</div>
