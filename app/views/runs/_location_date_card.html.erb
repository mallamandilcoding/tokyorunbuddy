<div class="d-flex run-date-card align-items-center justify-content-center mt-1 position-relative" id="run-date">
  <h1><strong><%= @run.start_time.strftime('%a, %b %e') %> at <%= @run.start_time.strftime('%H:%M') %></strong></h1>
  <span class="btn rounded-4  btn-outline-primary btn-sm" data-action="click->editpage#showeditpage" style="border:0; margin-bottom: 10px;"><i class="fa-regular fa-pen-to-square" style="font-size: 10px;"></i></span>
</div>
<div>
  <div class="d-flex justify-content-center my-1">
    <%= simple_form_for @run, remote: true, data: {action: "submit->scheduled#change", scheduled_target: "form",loader_target: "form" }, html:{class: "add-to-calendar"} do |f| %>
      <%= f.input :status, as: :hidden, wrapper_html: { id: "hidden-margin" }, input_html: {value: @scheduled ? "suggested" : 'scheduled', data: {scheduled_target: "inputvalue"}} %>
      <%= hidden_field_tag :authenticity_token, form_authenticity_token,  data: {scheduled_target: "token"}  %>
      <%= f.submit @scheduled ? "CANCEL" : 'ADD TO CALENDAR', class: 'btn rounded-4 btn-warning text-white btn-md ', data: { scheduled_target: "button"} %>
    <% end %>
  </div>
</div>
